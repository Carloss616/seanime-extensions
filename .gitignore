.DS_Store
node_modules/
*.log
__pycache__/
.idea/
.vscode/
.tsbuildinfo
.omc

# Per-developer dev manifests. They bake in an absolute filesystem path in
# `payloadURI` and `isDevelopment: true`, so they MUST NOT be shared — and
# seanime refuses to install them via the marketplace flow anyway.
*.dev.json

# Local design docs / implementation plans — kept on disk for reference
# but not part of the repo's public surface.
docs/superpowers/